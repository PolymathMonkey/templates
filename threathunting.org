#+TITLE: Threat Hunt: %^{Hunt Name}
#+DATE: %U
#+AUTHOR: polymathmonkey
#+STARTUP: overview
#+STARTUP: indent
#+OPTIONS: toc:2 num:nil
#+TAGS: active(a) closed(c) escalated(e) fp(f)
#+PROPERTY: header-args :results output :exports both
#+PROPERTY: header-args:shell :dir /ssh:siem1:
#+PROPERTY: header-args:python :python python3

# ============================================================
# THREAT HUNT TEMPLATE
# Usage: M-x org-capture → select TH template
# ES queries run via org-babel (C-c C-c on src block)
# ============================================================

* 📋 Hunt Metadata
:PROPERTIES:
:HUNT_ID:   TH-%<%Y%m%d>-001
:DATE:      %U
:ANALYST:   polymathmonkey
:STATUS:    In Progress
:TLP:       AMBER
:PAP:       AMBER
:MITRE_TTP:
:END:

** Classification
| Field         | Value                  |
|---------------+------------------------|
| Hunt ID       | TH-%<%Y%m%d>-001       |
| Start Date    | %<%Y-%m-%d>            |
| End Date      |                        |
| Analyst       | polymathmonkey         |
| Status        | In Progress            |
| TLP           | AMBER                  |
| Trigger       | [[*Trigger][see below]] |

** Trigger
# What triggered this hunt? MISP alert, anomaly, tip, etc.


** Scope
# Time range, data sources, assets in scope
- Time Range :: last 7 days (~now-7d~)
- Data Sources :: cowrie-*, webhoneypot-*
- Assets :: cowrie1 (192.168.210.3)

* 🎯 Hypothesis
# Fill in: "I believe [ACTOR] is [DOING X] using [TECHNIQUE] because [EVIDENCE]"

#+begin_quote
Hypothesis:

Confidence: LOW / MEDIUM / HIGH
#+end_quote

** ATT&CK Mapping
| Tactic      | Technique | ID | Notes |
|-------------+-----------+----+-------|
|             |           |    |       |

* 🔍 Investigation

** 1. Initial Scope Check

*** Event Volume (last 7 days)

#+begin_src shell
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {"range": {"@timestamp": {"gte": "now-7d"}}},
    "aggs": {"per_day": {"date_histogram": {"field": "@timestamp", "calendar_interval": "day"}}},
    "size": 0
  }' | python3 -m json.tool | grep -E "key_as_string|doc_count"
#+end_src

#+RESULTS:

*** MISP Hits in Scope

#+begin_src shell
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {
      "bool": {
        "must": [
          {"term": {"tags": "misp_hit"}},
          {"range": {"@timestamp": {"gte": "now-7d"}}}
        ]
      }
    },
    "size": 0
  }' | python3 -m json.tool | grep "value"
#+end_src

#+RESULTS:

*** Top Attacker IPs

#+begin_src shell
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {"range": {"@timestamp": {"gte": "now-7d"}}},
    "aggs": {
      "top_ips": {
        "terms": {"field": "source.address", "size": 20}
      }
    },
    "size": 0
  }' | python3 -m json.tool | grep -E '"key"|"doc_count"'
#+end_src

#+RESULTS:

** 2. MISP Intelligence

*** Known Threats Summary

#+begin_src shell
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {
      "bool": {
        "must": [
          {"term": {"tags": "misp_hit"}},
          {"range": {"@timestamp": {"gte": "now-7d"}}}
        ]
      }
    },
    "aggs": {
      "by_feed": {"terms": {"field": "misp.event_info", "size": 10}},
      "by_threat_level": {"terms": {"field": "misp.threat_level", "size": 5}},
      "unique_ips": {"cardinality": {"field": "source.address"}}
    },
    "size": 0
  }' | python3 -m json.tool
#+end_src

#+RESULTS:

*** MISP Misses (Unknown Threats)

#+begin_src shell
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {
      "bool": {
        "must": [
          {"range": {"@timestamp": {"gte": "now-7d"}}},
          {"exists": {"field": "source.address"}}
        ],
        "must_not": [
          {"term": {"tags": "misp_hit"}},
          {"prefix": {"source.address": "192.168."}}
        ]
      }
    },
    "aggs": {
      "unknown_ips": {
        "terms": {"field": "source.address", "size": 20}
      }
    },
    "size": 0
  }' | python3 -m json.tool | grep -E '"key"|"doc_count"'
#+end_src

#+RESULTS:

** 3. Specific IP Investigation
# Replace IP_HERE with target IP

#+NAME: target-ip
: IP_HERE

*** IP Activity Overview

#+begin_src shell :var ip=target-ip
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d "{
    \"query\": {\"term\": {\"source.address\": \"$ip\"}},
    \"sort\": [{\"@timestamp\": {\"order\": \"asc\"}}],
    \"size\": 100
  }" | python3 -m json.tool | grep -E "timestamp|event.outcome|user.name|user.password|user.command"
#+end_src

#+RESULTS:

*** Login Attempts for IP

#+begin_src shell :var ip=target-ip
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d "{
    \"query\": {
      \"bool\": {
        \"must\": [
          {\"term\": {\"source.address\": \"$ip\"}},
          {\"term\": {\"event.outcome\": \"login.failed\"}}
        ]
      }
    },
    \"aggs\": {
      \"usernames\": {\"terms\": {\"field\": \"user.name\", \"size\": 20}},
      \"passwords\": {\"terms\": {\"field\": \"user.password\", \"size\": 20}}
    },
    \"size\": 0
  }" | python3 -m json.tool
#+end_src

#+RESULTS:

*** Commands Executed (if any)

#+begin_src shell :var ip=target-ip
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d "{
    \"query\": {
      \"bool\": {
        \"must\": [
          {\"term\": {\"source.address\": \"$ip\"}},
          {\"exists\": {\"field\": \"user.command\"}}
        ]
      }
    },
    \"size\": 50
  }" | python3 -m json.tool | grep "user.command"
#+end_src

#+RESULTS:

*** MISP Direct Lookup

#+begin_src shell :var ip=target-ip
curl -s -k \
  -H "Authorization: YOUR_MISP_API_KEY" \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -X POST "https://192.168.210.8:8043/events/restSearch" \
  -d "{\"returnFormat\":\"json\",\"value\":\"$ip\",\"page\":1,\"limit\":5}" \
  | python3 -m json.tool | grep -E "info|threat_level|timestamp|org" | head -20
#+end_src

#+RESULTS:

*** External OSINT Links
# Click to open in browser

| Source     | Link                                                       |
|------------+------------------------------------------------------------|
| AbuseIPDB  | [[https://www.abuseipdb.com/check/IP_HERE]]                |
| GreyNoise  | [[https://viz.greynoise.io/ip/IP_HERE]]                    |
| VirusTotal | [[https://www.virustotal.com/gui/ip-address/IP_HERE]]      |
| Shodan     | [[https://www.shodan.io/host/IP_HERE]]                     |
| IPInfo     | [[https://ipinfo.io/IP_HERE]]                              |
| BGP.HE     | [[https://bgp.he.net/ip/IP_HERE]]                          |
| OTX        | [[https://otx.alienvault.com/indicator/ip/IP_HERE]]        |

*** OSINT Notes
# Paste relevant findings here


** 4. Attack Pattern Analysis

*** Username/Password Patterns (all IPs)

#+begin_src shell
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {
      "bool": {
        "must": [
          {"range": {"@timestamp": {"gte": "now-7d"}}},
          {"term": {"event.outcome": "login.failed"}}
        ]
      }
    },
    "aggs": {
      "top_usernames": {"terms": {"field": "user.name", "size": 20}},
      "top_passwords": {"terms": {"field": "user.password", "size": 20}},
      "top_combos": {
        "terms": {
          "script": "doc['\''user.name'\''].value + '\'':'\''" + doc['\''user.password'\''].value",
          "size": 20
        }
      }
    },
    "size": 0
  }' | python3 -m json.tool | grep -E '"key"|"doc_count"'
#+end_src

#+RESULTS:

*** Successful Logins

#+begin_src shell
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {
      "bool": {
        "must": [
          {"range": {"@timestamp": {"gte": "now-7d"}}},
          {"term": {"event.outcome": "login.success"}}
        ]
      }
    },
    "size": 50
  }' | python3 -m json.tool | grep -E "timestamp|source.address|user.name"
#+end_src

#+RESULTS:

*** Command Execution Patterns

#+begin_src shell
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {
      "bool": {
        "must": [
          {"range": {"@timestamp": {"gte": "now-7d"}}},
          {"exists": {"field": "user.command"}}
        ]
      }
    },
    "aggs": {
      "top_commands": {"terms": {"field": "user.command", "size": 30}}
    },
    "size": 0
  }' | python3 -m json.tool | grep -E '"key"|"doc_count"'
#+end_src

#+RESULTS:

*** Geographic Distribution

#+begin_src shell
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {"range": {"@timestamp": {"gte": "now-7d"}}},
    "aggs": {
      "by_country": {
        "terms": {"field": "source.geo.country_name", "size": 20}
      },
      "by_asn": {
        "terms": {"field": "source.geo.as.organization.name", "size": 20}
      }
    },
    "size": 0
  }' | python3 -m json.tool | grep -E '"key"|"doc_count"'
#+end_src

#+RESULTS:

** 5. SSH Client Fingerprinting (HASSH)

*** Top HASSH Fingerprints

#+begin_src shell
curl -s -k -u elastic:PASSWORD \
  "https://localhost:9200/cowrie-*/_search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {
      "bool": {
        "must": [
          {"range": {"@timestamp": {"gte": "now-7d"}}},
          {"exists": {"field": "ssh.client"}}
        ]
      }
    },
    "aggs": {
      "hassh": {"terms": {"field": "ssh.client", "size": 20}}
    },
    "size": 0
  }' | python3 -m json.tool | grep -E '"key"|"doc_count"'
#+end_src

#+RESULTS:

* 📊 Findings

** Summary Table

| # | IP | Country | MISP Feed | Threat Level | Commands | Status |
|---+----+---------+-----------+--------------+----------+--------|
| 1 |    |         |           |              |          |        |

** Finding Detail

*** Finding 1: [Title]                                               :active:
:PROPERTIES:
:SEVERITY: HIGH / MEDIUM / LOW / INFO
:CONFIDENCE: HIGH / MEDIUM / LOW
:MITRE: TA0001/T1110
:STATUS: Active
:END:

*Description:*


*Evidence:*
#+begin_src
# paste relevant log lines or query output here
#+end_src

*OSINT:*


*Assessment:*


*Recommendation:*


* ✅ Conclusions

** Hypothesis Result
# Was the hypothesis confirmed, rejected, or inconclusive?

☐ Confirmed
☐ Rejected
☐ Inconclusive

** Key Findings
1.
2.
3.

** False Positives Identified
-

** New Indicators (for MISP)
| Type   | Value | Context | Confidence |
|--------+-------+---------+------------|
| ip-src |       |         |            |

** Recommendations
- [ ]
- [ ]
- [ ]

* 📎 Evidence Appendix

** Raw Log Snippets
#+begin_src json

#+end_src

** Screenshots
# [[file:screenshots/hunt-001-kibana.png]]

** References
-
-

* 📝 Hunt Log
# Keep a running log of what you did and when

| Time | Action | Result |
|------+--------+--------|
|      |        |        |

* 🔄 Next Steps
- [ ]
- [ ]

# Local Variables:
# org-confirm-babel-evaluate: nil
# End:
